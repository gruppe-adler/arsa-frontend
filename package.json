{
  "name": "arsa-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "docker": "docker buildx build -t arsa-frontend .",
    "docker-run": "docker run -d --rm -p 80:80 --name arsa-frontend arsa-frontend"
  },
  "dependencies": {
    "is-ip": "^5.0.1",
    "pinia": "^2.2.2",
    "vue": "^3.4.37",
    "vue-router": "^4.4.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.1.2",
    "typescript": "^5.5.3",
    "vite": "^5.4.1",
    "vue-tsc": "^2.0.29"
  }
}
